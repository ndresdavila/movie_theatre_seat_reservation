// src/types/Room.ts
export interface Room {
    id: number;       // Se asume que todos los objetos del backend tienen un ID
    name: string;     // Nombre de la sala
    number: number;   // Número de la sala
}
